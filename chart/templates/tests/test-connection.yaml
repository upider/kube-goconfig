apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kube-goconfig.fullname" . }}-test-connection"
  labels:
    {{- include "kube-goconfig.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kube-goconfig.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
